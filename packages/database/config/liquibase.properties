# Liquibase configuration for DrugInsightAI database
changeLogFile=migrations/db.changelog-master.xml
driver=org.postgresql.Driver

# Local development settings (override with environment variables)
url=jdbc:postgresql://localhost:5432/druginsightai
username=druginsightai
password=changeme

# AWS RDS settings (use environment variables in production)
# url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
# username=${DB_USERNAME}
# password=${DB_PASSWORD}

# Liquibase settings
logLevel=INFO
outputDefaultSchema=false
outputDefaultCatalog=false

# Schema settings
defaultSchemaName=public
liquibaseSchemaName=public

# Logging
logFile=logs/liquibase.log

# Context settings for different environments
contexts=local,dev,staging,prod
